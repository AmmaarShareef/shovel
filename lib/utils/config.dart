import 'constants.dart';

// Set to true to use mock services instead of real API
const bool useMockServices = true; // Change to false when backend is ready

const String apiBaseUrl = AppConstants.apiBaseUrl;

