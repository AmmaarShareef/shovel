import 'package:flutter/material.dart';

class SetBoundaryScreen extends StatelessWidget {
  const SetBoundaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Set Service Area')),
      body: const Center(child: Text('Set Boundary Screen')),
    );
  }
}

